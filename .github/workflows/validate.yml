---
name: validate-tf

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  pull_request_target:
    branches:
      - main

jobs:
  validate-tf:
    uses: trussworks/shared-actions/.github/workflows/validate-tf.yml@commit-vaildate-tf
    secrets: inherit
